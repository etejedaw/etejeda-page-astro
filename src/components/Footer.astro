<footer class="footer">
	<div class="container">
		<div class="footer-content">
			<span class="footer-brand">
				<span class="bracket">{"{ "}</span>etejeda<span class="bracket">
					{"};"}</span
				>
			</span>
			<div class="footer-links">
				<a href="mailto:contacto@etejeda.dev">Email</a>
				<a
					href="https://www.linkedin.com/in/etejedaw/"
					target="_blank"
					rel="noopener noreferrer">LinkedIn</a
				>
				<a
					href="https://github.com/etejedaw"
					target="_blank"
					rel="noopener noreferrer">GitHub</a
				>
			</div>
			<span class="footer-bridge">
				¿Servicios para pymes? → <a
					href="https://www.estebantejeda.com"
					target="_blank"
					rel="noopener noreferrer">estebantejeda.com</a
				>
			</span>
		</div>
	</div>
</footer>

<style>
	.footer {
		padding: 2rem 1.5rem;
		border-top: 1px solid rgba(var(--overlay-rgb), 0.06);
	}

	.container {
		max-width: 1100px;
		margin: 0 auto;
	}

	.footer-content {
		display: grid;
		grid-template-columns: 1fr auto 1fr;
		align-items: center;
		gap: 1.5rem;
	}

	.footer-brand {
		font-family: var(--font-jetbrains-mono), "Fira Code", monospace;
		font-weight: 700;
		color: var(--text-secondary);
		font-size: 0.95rem;
	}

	.bracket {
		color: var(--accent);
	}

	.footer-links {
		display: flex;
		gap: 1.5rem;
		justify-self: center;
	}

	.footer-links a {
		color: var(--text-muted);
		text-decoration: none;
		font-size: 0.85rem;
		transition: color 0.2s ease;
	}

	.footer-links a:hover {
		color: var(--accent);
	}

	.footer-bridge {
		color: var(--text-muted);
		font-size: 0.8rem;
		justify-self: end;
		text-align: right;
	}

	.footer-bridge a {
		color: var(--text-secondary);
		text-decoration: none;
		transition: color 0.2s ease;
	}

	.footer-bridge a:hover {
		color: var(--accent);
	}

	@media (max-width: 768px) {
		.footer-content {
			grid-template-columns: 1fr;
			gap: 0.75rem;
			text-align: center;
		}

		.footer-links,
		.footer-bridge {
			justify-self: center;
			text-align: center;
		}
	}
</style>
